extends base.pug
include mixins/video

block content
    form(method="GET").search_from
        input(placeholder="Search by title", name="keyword", type="text")
            button 
                i.fas.fa-search
        div.video-grid
            each video in videos 
                +video(video)
            else 
                span.empty__message No Videos Found
            